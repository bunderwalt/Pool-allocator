# Pool Allocator 🧠

Перед прочтением советую хорошенько познакомиться с выравниванием памяти, виртуальной памятью. Для понимания прицнипа работы linear allocator можно прочитать это статью https://habr.com/ru/articles/505632/


Простой и эффективный пул-аллокатор на языке C, реализующий стратегию управления памятью "пул объектов".

## 📋 О проекте

Пул-аллокатор выделяет большой блок памяти один раз и затем обслуживает запросы на выделение/освобождение памяти из этого предварительно выделенного пула. Это обеспечивает:

- **Высокую производительность** - исключаются многократные системные вызовы
- **Предсказуемое время выполнения** - операции за O(1)
- **Отсутствие фрагментации** - память организована блоками фиксированного размера
- **Локальность данных** - последовательное расположение в памяти улучшает кэширование



# Pool Allocator

Простой и эффективный пул-аллокатор на языке C с фиксированными блоками памяти.

## Структуры данных

### Структура Chunk
| Поле | Тип | Размер | Описание |
|------|-----|--------|-----------|
| `next_adress` | `struct Chunk*` | 8 байт | Указатель на следующий чанк в списке свободных |
| `used` | `bool` | 1 байт | Флаг использования чанка |

### Структура PoolAllocator
| Поле | Тип | Размер | Описание |
|------|-----|--------|-----------|
| `start_pool` | `void*` | 8 байт | Указатель на начало пула памяти |
| `free_list` | `Chunk*` | 8 байт | Голова списка свободных чанков |
| `chunk_size` | `size_t` | 8 байт | Размер одного чанка (заголовок + данные) |
| `chunk_count` | `size_t` | 8 байт | Общее количество чанков |
| `pool_size` | `size_t` | 8 байт | Общий размер выделенной памяти |

## Организация памяти

### Макросы и константы
| Имя | Значение | Описание |
|-----|----------|-----------|
| `ALIGNMENT` | 8 | Выравнивание для всех структур |
| `ALIGN(size)` | `(((size) + 7) & ~7)` | Макрос для выравнивания размеров |
| `PAGE_SIZE` | 4096 | Размер страницы памяти |

### Распределение виртуальной памяти
| Область | Смещение | Размер | Содержимое |
|---------|----------|--------|------------|
| **Заголовок аллокатора** | 0 | `sizeof(PoolAllocator)` | Метаданные пула |
| **Пул чанков** | `sizeof(PoolAllocator)` | `chunk_size * chunk_count` | Данные всех чанков |
| **Неиспользуемая память** | `sizeof(PoolAllocator) + chunk_size * chunk_count` | `aligned_size - total_size` | Выравнивание до границы страницы |

### Структура чанка в памяти
| Смещение | Размер | Содержимое | Описание |
|----------|--------|------------|-----------|
| 0 | 8 байт | `next_adress` | Указатель на следующий свободный чанк |
| 8 | 1 байт | `used` | Флаг использования |
| 9 | 7 байт | *padding* | Выравнивание до 8 байт |
| 16 | `chunk_size - 16` | **Данные пользователя** | Полезная нагрузка |

## API функции

### Основные функции
| Функция | Параметры | Возвращает | Описание |
|---------|-----------|------------|-----------|
| `allocator_init` | `chunk_data_size`, `chunk_count` | `PoolAllocator*` | Создает и инициализирует аллокатор |
| `pool_alloc` | `allocator` | `void*` | Выделяет один чанк памяти |
| `chunk_free` | `allocator`, `ptr` | `void` | Освобождает чанк памяти |
| `pool_destroy` | `allocator` | `void` | Уничтожает аллокатор и освобождает память |

### Вспомогательные функции
| Функция | Параметры | Возвращает | Описание |
|---------|-----------|------------|-----------|
| `get_available_chunks` | `allocator` | `size_t` | Количество свободных чанков |
| `is_pointer_valid` | `allocator`, `ptr` | `bool` | Проверяет валидность указателя |

## Процесс работы

### Инициализация аллокатора
| Шаг | Действие | Результат |
|-----|----------|-----------|
| 1 | Расчет размеров | `chunk_size = ALIGN(sizeof(Chunk) + data_size)` |
| 2 | Выделение памяти | `mmap()` с выравниванием на страницу |
| 3 | Инициализация структуры | Заполнение полей PoolAllocator |
| 4 | Создание свободного списка | Связывание чанков в обратном порядке |

### Выделение памяти (pool_alloc)
| Шаг | Действие | Сложность |
|-----|----------|-----------|
| 1 | Проверка `free_list` | O(1) |
| 2 | Извлечение первого чанка | O(1) |
| 3 | Обновление `free_list` | O(1) |
| 4 | Установка флага `used` | O(1) |
| 5 | Возврат указателя на данные | O(1) |

### Освобождение памяти (chunk_free)
| Шаг | Действие | Сложность |
|-----|----------|-----------|
| 1 | Проверка параметров | O(1) |
| 2 | Получение заголовка чанка | O(1) |
| 3 | Проверка на double-free | O(1) |
| 4 | Сброс флага `used` | O(1) |
| 5 | Добавление в `free_list` | O(1) |

## Пример использования памяти

### Параметры примера
| Параметр | Значение | Расчет |
|----------|----------|--------|
| `chunk_data_size` | 64 байта | Размер данных пользователя |
| `chunk_count` | 10 | Количество чанков |
| `sizeof(Chunk)` | 16 байт | Заголовок чанка |
| `chunk_size` | 80 байт | `ALIGN(16 + 64)` |
| `sizeof(PoolAllocator)` | 48 байт | Заголовок аллокатора |
| `total_size` | 848 байт | `48 + 80 * 10` |
| `aligned_size` | 4096 байт | Выравнивание до границы страницы |

### Распределение чанков
| Чанк | Адрес данных | Размер | Состояние после инициализации |
|------|-------------|--------|-------------------------------|
| 0 | `base + 64` | 80 байт | Свободен |
| 1 | `base + 144` | 80 байт | Свободен |
| 2 | `base + 224` | 80 байт | Свободен |
| ... | ... | ... | ... |
| 9 | `base + 864` | 80 байт | Свободен |

## Характеристики производительности

### Преимущества
| Характеристика | Эффект | Причина |
|----------------|--------|---------|
| Быстрое выделение | O(1) | Простое извлечение из списка |
| Нет фрагментации | Стабильная производительность | Фиксированные блоки |
| Локальность данных | Улучшение кэширования | Последовательное расположение |
| Предсказуемость | Детерминированное время | Отсутствие поиска |

### Ограничения
| Ограничение | Влияние | Обходное решение |
|-------------|---------|------------------|
| Фиксированный размер | Неэффективно для разных размеров | Создание нескольких пулов |
| Статическое количество | Ограниченное число объектов | Оценка максимальной нагрузки |
| Overhead | Потеря памяти на заголовки | Увеличение размера данных |

## Сравнение с другими аллокаторами

| Характеристика | Пул-аллокатор | `malloc` | Slab-аллокатор |
|----------------|---------------|----------|----------------|
| Скорость выделения | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Фрагментация | Нет | Есть | Минимальная |
| Память под overhead | Фиксированная | Переменная | Фиксированная |
| Гибкость размеров | Низкая | Высокая | Средняя |
| Сложность реализации | Простая | Сложная | Средняя |


## 📞 Способ связи со мной:
Почта - bunderwalt@gmail.com
Telegram - Bunderwalt


